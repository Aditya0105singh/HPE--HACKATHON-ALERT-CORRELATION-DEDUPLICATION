fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
scikit-learn>=1.4
numpy>=1.26
scipy>=1.11
networkx>=2.8
drain3>=0.9.11   # log template mining (engine falls back to a regex template if absent)
sqlalchemy>=2.0
# Optional: pip install presidio-analyzer spacy && python -m spacy download en_core_web_sm  (NER redaction; regex redaction always runs)

# Test-only
pytest>=8.0
httpx>=0.27
