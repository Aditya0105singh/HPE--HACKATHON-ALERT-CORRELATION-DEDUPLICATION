import Extraction from "./extraction";

export default function Page() {
  return <Extraction />;
}

export const metadata = {
  title: "Event Extraction | AlertLens",
  description:
    "Map new event attributes from existing event attributes with Regex",
};
