import Mapping from "./mapping";

export default function Page() {
  return <Mapping />;
}

export const metadata = {
  title: "Event Mapping | AlertLens",
  description: "Add dynamic context to your alerts with mapping",
};
